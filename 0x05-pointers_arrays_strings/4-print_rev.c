#include "main.h"
/**
 * print_rev -  function that prints a string
 * @s: string
 * Return: Always(0)
 */
void print_rev(char *s);
int longi = 0;

while (*s)
{
s++;
count++;
}

while (count)
{
s--;
_putchar(*s);
count--;
}

_putchar('\n');
}
