#include "main.h"

/**
 * print_last_digit - function 'print_last_digit' that prints the last digit of a number
 *@i : function parameter
 * Return: Always 0.
 */
int print_last_digit(int n);
{
int k;
k = i % 10;
if (i < 0)
k = -k;
return (k);
}
