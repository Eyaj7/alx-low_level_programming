#include "main.h"
#include <stdio.h>

/**
 * _abs - function that computes the absolute value of an integer
 * @n: integer type
 *
 * Return: Always 0.
 */
int _abs(int n)
if (n < 0)
n = (-1) * n;
else if (n >= 0)
n == n;
return (n);
{
